---
title: "Documentation"
summary: "Custom components and markdown syntax extensions for building rich content in Raksara"
---

# Documentation

Raksara extends standard Markdown with a set of built-in components. Each component uses a simple syntax that blends naturally into your writing — no HTML or complex configuration required.

All components are rendered client-side and styled with the site's glassmorphism theme.

::component(pages/doc)
